//! Execution-time inspection: the fixed L-Lang `inspect` CLI report, its persistence and the
//! finite projection/Wasm comparison. Candidate JavaScript is never an entrypoint.

pub mod comparison;
pub mod contracts;
pub mod repository;
pub mod schema;
pub mod service;
