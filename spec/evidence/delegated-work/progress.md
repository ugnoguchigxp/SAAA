# Delegated work progress

| Card | Status | Evidence |
| --- | --- | --- |
| DW-00 | complete | `baseline.md` records the pre-existing trigger and lifecycle boundaries. |
| DW-01 | complete | `steward/contracts.rs` validates operations, verifier choices, and bounded proposals. |
| DW-02 | complete | Additive steward columns, origin bindings, reservations table, and 8-goal limit are migrated. |
| DW-03 | complete | `work_propose/status/amend/withdraw` are exposed through both the standard streamed and agent-session SSE coding bridges. The host derives the source message from the current persisted run, validates it, persists a proposal, and queues the fixed read/test recipe. |
| DW-04 | complete | Panel distinguishes goal state, scope/budget/verifier/notification, supports per-goal withdrawal, and exposes the bounded per-goal notification amendment. Direct registration requires an explicit confirmation over the user-selected summary, read/test operation, bounded run/time budget, verifier, and notification route; the host revalidates and persists precisely that scope. Scope/budget changes deliberately require a new confirmed proposal rather than widening an existing authority. |
| DW-08 | complete | `steward_event_cursor` consumes committed Coding events in the runner transaction. |
| DW-10 | partial | Test-report versus tests-pass verifiers do not share a completion state. Goal plans and their finite step/dependency rows are durable for new and migrated Goals. `read_test` runs as durable sequential work: a settled `read` Task commits exactly one dependent `test_run` Task with a distinct dispatch intent, and the report/event wake-up starts queued work without a new user turn. Arbitrary `TaskPlan` graph execution, outcome-artifact references, and bounded replanning remain pending. |
| DW-11 | complete | Reports are durable and deduplicated; a UI refresh delivers them without a user turn and respects Situation hold. |
| DW-07 | blocked-by-runtime | `delegated-read-test-macos-v1` enforces workspace-write and network default deny with temporary/session output allowed. Node 24 required the read-only Seatbelt permission `sysctl-read` during allocator initialization; after adding only that permission, the installed Pi 0.86.1 starts and answers an RPC state request. A real Codex SDK turn still needs its home state DB and in-process app-server writes; granting those would violate the profile, so the SDK combination is deliberately unsupported and not exposed in the UI. A supported authenticated read/test adapter remains required. |
| DW-05 | complete | `coding_origin_bindings` distinguishes user turns from delegated events. Delegated starts use a task-scoped synthetic run key and are authorized/revoked through the active steward task. The production service fixture executes a delegated event end-to-end, records `delegated_event/task` as its sole origin, and creates no user message at dispatch. |
| DW-06 | complete | Dispatch intents and restart-to-unknown recovery are persisted. Both user-triggered and schedule-triggered delegated work use the common Coding service. An accepted receipt is durable; on restart, an unreceived `dispatching` intent becomes `outcome_unknown` and cannot be reclaimed or replayed. |
| DW-09 | complete | Schedule TaskRun verifies durable `task:<id>` and delegation ids through the steward dispatcher, then records Started only after its receipt. Window expiry becomes `missed` before dispatch; a restart closes a durable `firing` TaskRun for inspection without replay. |
| DW-13 | complete | Withdraw/forget cancel derived tasks, release unconsumed reservation, request stop for a running Coding job before source deletion, and ignore late terminal events. The production-adapter fixture now deletes an accepted run's source and confirms both the live fixture process/run and its job reach `interrupted`; a forgotten lineage cannot be continued. |
| DW-12 | partial | Report outbox rows now persist whether speech was requested and their speech delivery state. TTS begins only after the conversation-message commit and passes the existing Situation/global speech policy; a restart converts `starting`/`playback_started` to `delivery_unknown` with no automatic replay. The task-list IPC and panel expose durable delivery/speech state, so polling reconnects to the persisted status. A real audible delivery acceptance remains pending. |
| DW-14 | pending | Requires multiple-Goal, topic-switch, competition, sleep/restart acceptance through a real restricted execution profile. |

No user content, absolute workspace paths, or credentials are recorded here.
