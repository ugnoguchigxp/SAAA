# Delegated work progress

| Card | Status | Evidence |
| --- | --- | --- |
| DW-00 | complete | `baseline.md` records the pre-existing trigger and lifecycle boundaries. |
| DW-01 | complete | `steward/contracts.rs` validates operations, verifier choices, and bounded proposals. |
| DW-02 | complete | Additive steward columns, origin bindings, reservations table, and 8-goal limit are migrated. Both model proposals and the direct UI registration route enforce the same active-Goal limit. |
| DW-03 | complete | `work_propose/status/amend/withdraw` are exposed through both the standard streamed and agent-session SSE coding bridges. The host derives the source message from the current persisted run, validates it, persists a proposal, and queues the fixed read/test recipe. |
| DW-04 | complete | Panel distinguishes goal state, scope/budget/verifier/notification, supports per-goal withdrawal, and exposes the bounded per-goal notification amendment. Direct registration requires an explicit confirmation over the user-selected summary, read/test operation, bounded run/time budget, verifier, and notification route; the host revalidates and persists precisely that scope. A component acceptance verifies the exact confirmed payload. Scope/budget changes deliberately require a new confirmed proposal rather than widening an existing authority. |
| DW-08 | complete | `steward_event_cursor` consumes committed Coding events in the runner transaction. |
| DW-10 | complete | Test-report versus tests-pass verifiers do not share a completion state. Goal plans and finite step/dependency rows are durable for new and migrated Goals. Queueing reads the persisted dependency graph; a settled step commits exactly one ready successor with a distinct dispatch intent, and the report/event wake-up starts queued work without a new user turn. Failed work creates a copied plan revision at most twice; `interrupted`/unknown work does not replan. Terminal Task artifact references are durable and exposed through task-list IPC and the panel. |
| DW-11 | complete | Reports are durable and deduplicated; a UI refresh delivers them without a user turn and respects Situation hold. |
| DW-07 | complete | `delegated-read-test-macos-v1` keeps its macOS deny-by-default profile for recipe execution. `delegated-codex-sdk-macos-v1` uses a separate trusted Pi adapter: its mutable state is Git-ignored workspace-local `.saaa/delegated-sdk-state`, `CODEX_HOME` is fixed there, and auth is linked without copying it. The adapter must remain outside the outer Seatbelt because macOS rejects nested Seatbelt application (`sandbox_apply: EPERM`); model-generated tools instead run in the SDK's `read-only` sandbox with `sandbox_permissions: []`, so no extra filesystem roots are granted. Authenticated live checks confirmed workspace README read and durable-session recovery, rejected a workspace write, rejected a public-network request, and rejected reading a marker outside the workspace. |
| DW-05 | complete | `coding_origin_bindings` distinguishes user turns from delegated events. Delegated starts use a task-scoped synthetic run key and are authorized/revoked through the active steward task. The production service fixture executes a delegated event end-to-end, records `delegated_event/task` as its sole origin, and creates no user message at dispatch. |
| DW-06 | complete | Dispatch intents and restart-to-unknown recovery are persisted. Both user-triggered and schedule-triggered delegated work use the common Coding service. An accepted receipt is durable; on restart, an unreceived `dispatching` intent becomes `outcome_unknown` and cannot be reclaimed or replayed. |
| DW-09 | complete | Schedule TaskRun verifies durable `task:<id>` and delegation ids through the steward dispatcher, then records Started only after its receipt. Window expiry becomes `missed` before dispatch; a restart closes a durable `firing` TaskRun for inspection without replay. |
| DW-13 | complete | Withdraw/forget cancel derived tasks, release unconsumed reservation, request stop for a running Coding job before source deletion, and ignore late terminal events. The production-adapter fixture now deletes an accepted run's source and confirms both the live fixture process/run and its job reach `interrupted`; a forgotten lineage cannot be continued. |
| DW-12 | partial | Report outbox rows now persist whether speech was requested and their speech delivery state. TTS begins only after the conversation-message commit and passes the existing Situation/global speech policy; a restart converts `starting`/`playback_started` to `delivery_unknown` with no automatic replay. The task-list IPC and panel expose durable delivery/speech state, so polling reconnects to the persisted status. The current macOS host can run the selected System TTS prerequisites (`say` and `afplay`), but a real app-driven audible delivery acceptance remains pending. |
| DW-14 | partial | The restricted SDK profile has a live acceptance and desktop smoke now completes build, bundle, launch, IPC-ready observation, and cleanup. A browser-independent panel acceptance renders multiple Goals and verifies that withdrawing B routes only B's id to the host. A combined steward acceptance also covers topic switch, targeted withdrawal, hold, and release. The remaining final acceptance is the same flow exercised through the live UI with real execution competition and sleep/restart. |

No user content, absolute workspace paths, or credentials are recorded here.
